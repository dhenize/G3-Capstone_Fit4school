import { Redirect } from "expo-router";

export default function Index() {
    return <Redirect href="dash_mod/home" />; 
    // HOMESCREEN: dash_mod/home | CURRENTLY WORKING: ar_mod/ar_height, ar_mod/ar_result, items_mod/boys_unif_p | MAIN SCREEN DISPLAY, just change it when debugging
}

