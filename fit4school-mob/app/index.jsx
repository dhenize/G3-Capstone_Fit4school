import { Redirect } from "expo-router";

export default function Index() {
    return <Redirect href="dash_mod/home" />; // CURRENTLY WORKING: items_mod/boys_unif | MAIN SCREEN DISPLAY, just change it when debugging
}

