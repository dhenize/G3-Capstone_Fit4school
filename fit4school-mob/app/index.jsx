import { Redirect } from "expo-router";

export default function Index() {
    return <Redirect href="acc_mod/signup" />; // CURRENTLY WORKING: items_mod/boys_unif_p | MAIN SCREEN DISPLAY, just change it when debugging
}

