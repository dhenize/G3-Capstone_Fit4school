import { Redirect } from "expo-router";

export default function Index() {
    return <Redirect href="items_mod/boys_unif_p" />; // CURRENTLY WORKING: dash_mod/home | MAIN SCREEN DISPLAY, just change it when debugging
}

